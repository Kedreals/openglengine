#include "baseGame.hpp"


/** \mainpage Our OpenGL Engine

    \section intro_sec Introduction

    This is a little hobby project of ours.
    We try to build a little Game Engine using 
    OpenGL. We are both not familiar with OpenGL,
    but we will try our best to build an engine ^^.
 */

/** \brief just build a game and start.
 */
int main()
{
  BaseGame b;
  b.Start();
  return 0;
}
